"use client";

'use client';

import React, { useState } from 'react';
import AuthButtons from "@/components/AuthButtons";

const Navbar: React.FC = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <nav className="fixed top-0 left-0 right-0 z-50 glass-nav border-b border-[#E7EAF2]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-20">
          {/* Logo */}
          <div className="flex-shrink-0 flex items-center gap-2">
            <div className="w-10 h-10 bg-[#11B718] rounded-xl flex items-center justify-center">
              <span className="text-white font-black text-xl">S</span>
            </div>
            <span className="text-2xl font-extrabold tracking-tight text-[#0B1220]">
              SantiLink
            </span>
          </div>

          {/* Desktop Menu */}
          <div className="hidden md:flex items-center space-x-8">
            <a href="#features" className="text-sm font-semibold text-[#0B1220] hover:text-[#11B718] transition-colors">
              Funciones
            </a>
            <a href="#pricing" className="text-sm font-semibold text-[#0B1220] hover:text-[#11B718] transition-colors">
              Precios
            </a>
            <a href="#templates" className="text-sm font-semibold text-[#0B1220] hover:text-[#11B718] transition-colors">
              Plantillas
            </a>
            <a href="#" className="text-sm font-semibold text-[#0B1220] hover:text-[#11B718] transition-colors">
              Iniciar sesiÃ³n
            </a>
            <a
              href="#pricing"
              className="bg-[#11B718] text-white px-6 py-3 rounded-[16px] font-bold text-sm hover:shadow-lg hover:shadow-[#11B718]/30 transition-all active:scale-95 focus-visible:outline focus-visible:outline-2 focus-visible:outline-[#11B718]"
            >
              Crear gratis
            </a>
          </div>

          {/* Mobile Menu Button */}
          <div className="md:hidden flex items-center">
            <button
              onClick={() => setIsOpen(!isOpen)}
              className="p-2 rounded-lg text-[#0B1220] focus:outline-none"
              aria-label="Abrir menÃº"
            >
              <svg className="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                {isOpen ? (
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
                ) : (
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
                )}
              </svg>
            </button>
          </div>
        </div>
      </div>

      {/* Mobile Menu Panel */}
      {isOpen && (
        <div className="md:hidden bg-white border-b border-[#E7EAF2] px-4 py-6 space-y-4">
          <a href="#features" className="block text-lg font-semibold" onClick={() => setIsOpen(false)}>
            Funciones
          </a>
          <a href="#pricing" className="block text-lg font-semibold" onClick={() => setIsOpen(false)}>
            Precios
          </a>
          <a href="#templates" className="block text-lg font-semibold" onClick={() => setIsOpen(false)}>
            Plantillas
          </a>
          <a href="#" className="block text-lg font-semibold">
            Iniciar sesiÃ³n
          </a>
          <a href="#pricing" className="w-full block text-center bg-[#11B718] text-white py-4 rounded-[16px] font-bold text-lg">
            Crear gratis
          </a>
        </div>
      )}
      <div className="ml-auto"><AuthButtons /></div>
</nav>
  );
};

export default Navbar;
